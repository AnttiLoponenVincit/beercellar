import React from 'react';
import ReactDOM from 'react-dom';

// Inject the application to the HTML page
ReactDOM.render(
	<div>
		<h1>Beer Cellar</h1>
	</div>,
	document.getElementById('application')
);